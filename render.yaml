services:
  - type: web
    name: Catalogo-Peliculas-UDGvitual-docker
    plan: free
    region: oregon
    docker:
      dockerfilePath: ./Dockerfile
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: NODE_ENV
        value: production

